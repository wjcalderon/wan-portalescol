name: Build and publish docker image - prod

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

env:
  AWS_REGISTRY: 590184135173.dkr.ecr.us-east-1.amazonaws.com
  GH_REGISTRY: ghcr.io
  IMAGE_NAME: hdiprod
  REGION: us-east-1

jobs:
  build_and_publish:
    name: Build and publish prod
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: read
      id-token: write

    steps:

      - name: Check out the repo
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Pull base image
        run: |
          echo ${{ secrets.GITHUB_TOKEN }} | docker login ${{ env.GH_REGISTRY }} -u ${{ github.actor }} --password-stdin
          docker pull ghcr.io/hdiseguroscol/php-nginx-docker:v1.0.4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          registry: ${{ env.AWS_REGISTRY }}
          username: ${{ secrets.PROD_KEY_ID }}
          password: ${{ secrets.PROD_ACCESS_KEY }}
          logout: true

      - name: Install dependencies
        uses: php-actions/composer@v6
        with:
          php_version: "8.1"
          version: "2.2"
          args: "--ignore-platform-reqs --optimize-autoloader"

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.AWS_REGISTRY }}/${{ env.IMAGE_NAME }}
          flavor: |
            latest=false

      - name: Build and push
        id: push
        uses: docker/build-push-action@v5
        with:
          context: .
          platforms: linux/arm64,linux/amd64
          provenance: false
          push: true
          tags: ${{ steps.meta.outputs.tags }}
