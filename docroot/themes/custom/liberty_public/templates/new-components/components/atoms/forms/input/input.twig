<input
    id="{{input_id}}"
    type="{{ input_type }}" 
    value="" 
    placeholder="{{ input_placeholder }}" 
    required="{{ input_required }}"
    size="{{input_size}}" 
    maxlength="{{input_maxlength}}" 
>