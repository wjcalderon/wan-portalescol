{#
/**
 * @file
 * Default theme implementation to display a paragraph.
 *
 * Available variables:
 * - paragraph: Full paragraph entity.
 *   Only method names starting with "get", "has", or "is" and a few common
 *   methods such as "id", "label", and "bundle" are available. For example:
 *   - paragraph.getCreatedTime() will return the paragraph creation timestamp.
 *   - paragraph.id(): The paragraph ID.
 *   - paragraph.bundle(): The type of the paragraph, for example, "image" or "text".
 *   - paragraph.getOwnerId(): The user ID of the paragraph author.
 *   See Drupal\paragraphs\Entity\Paragraph for a full list of public properties
 *   and methods for the paragraph object.
 * - content: All paragraph items. Use {{ content }} to print them all,
 *   or print a subset such as {{ content.field_example }}. Use
 *   {{ content|without('field_example') }} to temporarily suppress the printing
 *   of a given child element.
 * - attributes: HTML attributes for the containing element.
 *   The attributes.class element may contain one or more of the following
 *   classes:
 *   - paragraphs: The current template type (also known as a "theming hook").
 *   - paragraphs--type-[type]: The current paragraphs type. For example, if the paragraph is an
 *     "Image" it would result in "paragraphs--type--image". Note that the machine
 *     name will often be in a short form of the human readable label.
 *   - paragraphs--view-mode--[view_mode]: The View Mode of the paragraph; for example, a
 *     preview would result in: "paragraphs--view-mode--preview", and
 *     default: "paragraphs--view-mode--default".
 * - view_mode: View mode; for example, "preview" or "full".
 * - logged_in: Flag for authenticated user status. Will be true when the
 *   current user is a logged-in member.
 * - is_admin: Flag for admin user status. Will be true when the current user
 *   is an administrator.
 *
 * @see template_preprocess_paragraph()
 *
 * @ingroup themeable
 */
#}


{%
  set classes = [
    'component',
    'component__' ~ paragraph.bundle|clean_class,
  ]
%}

{# Container by default. #}
{% set add_container = true %}

{# Width. #}
{% if content.field_c_width|render %}
  {% set c_width = content.field_c_width['#items'].getString() %}
    {% if c_width == 'edge2edge' %}
      {% set add_container = false %}
    {% endif %}
{% endif %}

{# Sets Unique ID for paragraph from Paragraph ID. #}
{% set paragraph_id = 'component-' ~ paragraph.id.value %}

{# Sets paragraph title for paragraph. #}
{% set paragraph_title = content.field_c_title|render %}

{# Sets paragraph title status for paragraph. #}
{% set paragraph_title_status = true %}
{% if 'On' in content.field_c_title_status[0] %}
  {% set paragraph_title_status = false %}
{% endif %}

{# Sets paragraph subtitle for paragraph. #}
{% set paragraph_subtitle = content.field_c_subtitle|field_value %}

{# Sets paragraph lead for paragraph. #}
{% set paragraph_lead = content.field_lead|field_value %}

{# Add custom paragraph CSS class(es). #}
{%  set c_clasess = content.field_c_classes|render %}
{% set js_slider_class = false %}
{% set select_mobile_class = false %}
{% set accordion_mobile_class = false %}

{% if c_clasess %}
  {% set striped_custom_classes = c_clasess|striptags|lower %}
  {% set split_custom_classes = striped_custom_classes|split(' ') %}
  {% for custom_class in split_custom_classes %}

    {% if custom_class|trim == 'component__card--cifras' %}
      {% set js_slider_class = 'js-slider' %}
    {% endif %}
    {% if custom_class|trim == 'component__tab--content-select-mb' %}
      {% set select_mobile_class = true %}
    {% endif %}
    {% if custom_class|trim == 'component__tab--content-accordion-mb' %}
      {% set accordion_mobile_class = true %}
    {% endif %}

    {% set clened_custom_class = [ custom_class|clean_class ] %}
    {% set classes = classes|merge(clened_custom_class) %}
  {%  endfor %}
{% endif %}

{% set class_container = '' %}
{% if add_container %}
   {% set class_container = class_container~ ' '~ 'cog--mq' %}
{% endif %}

{% set tabs = content.field_multi_tab_item %}


<section{{ attributes.addClass(classes).setAttribute('id', paragraph_id) }} >
    {% if paragraph_title and paragraph_title_status %}
      <div class="component__heading">
          {% if add_container %}<div class="cog--mq">{% endif %}
              {% if paragraph_title is not empty %}
                <h2 class="component__title">{{- paragraph_title -}}</h2>
              {% endif %}
              {% if paragraph_subtitle is not empty %}
                <h3 class="component__subtitle">{{- paragraph_subtitle -}}</h3>
              {% endif %}
              {% if paragraph_lead is not empty %}
                <p class="lead"> {{- paragraph_lead -}} </p>
              {% endif %}
          {% if add_container %}</div>{% endif %}
      </div>
    {% endif %}
  <div class="component__content">
        <div id="tabs" class="cp-tabs">
            <div class="component__heading">
              <div class="cog--mq">
                  <ul class="js-slider" data-slick='{"slidesToShow": 4, "slidesToScroll": 1, "responsive": [{"breakpoint": 978,"settings": {"slidesToShow": 4,"slidesToScroll": 1,"dots": false,"arrows": true}}]}'>
                    {% for key, item in tabs['#items'] %}
                      <li>
                        <a href='#tabs-{{ key }}'>
                          {% if item.entity.field_image is not empty %}
<<<<<<< HEAD
                            <img src="{{ file_url(item.entity.field_image.entity.fileuri)}}">
=======
<img alt="" src="{{ file_url(item.entity.field_image.entity.fileuri)}}">

>>>>>>> main
                          {% endif %}
                          {% if item.entity.field_name is not empty %}
                            {{ item.entity.field_name.value }}
                          {% endif %}
                        </a>
                      </li>
                    {% endfor %}
                  </ul>
              </div>
            </div>
            {% for key, item in tabs['#items'] %}
              {% set items_contenido = item.entity.field_body_item_tab_multiple %}
              {% set flag_subtabs = false %}
              <div id="tabs-{{ key }}">
                <div id="sub-tabs" class="cp-tabs sub-tabs">
                  <div class="component__heading">
                    <div class="cog--mq">
                      <ul>
                        {% for key_contenido ,item_contenido in items_contenido %}
                          {% if item_contenido.entity.bundle() == 'item_tab_multi' %}
                             {% set flag_subtabs = true %}
                            <li><a id="enlace-sub-tabs-{{ key }}-{{ key_contenido }}" href='#sub-tabs-{{ key }}-{{ key_contenido }}'>
                              {{ item_contenido.entity.field_name.value }}
                            </a>
                            </li>
                          {% endif %}
                        {% endfor %}
                      </ul>
                    </div>
                  </div>

                  {% if select_mobile_class %}
                    <div class="component__heading--mb">
                      <div class="cog--mq">
                        <div class="component__select--mb">
                          <p>Selecciona una categoría</p>
                          <div class="form-item">
                            <select class="select--tabs">
                              {% for key_contenido ,item_contenido in items_contenido %}
                                {% if item_contenido.entity.bundle() == 'item_tab_multi' %}
                                  {% set flag_subtabs = true %}
                                    <option value="enlace-sub-tabs-{{ key }}-{{ key_contenido }}">
                                      {{ item_contenido.entity.field_name.value }}
                                    </option>
                                {% endif %}
                              {% endfor %}
                            </select>
                          </div>
                        </div>
                      </div>
                    </div>
                  {% endif %}

                  <div class="component__content">
                    <div class="cog--mq cog--flex">
                      {% if flag_subtabs %}
                        {% for key_contenido ,item_contenido in items_contenido %}
                          {% if item_contenido.entity.bundle() == 'item_tab_multi' %}
                            <div id="sub-tabs-{{ key }}-{{ key_contenido }}" class="sub-tab__container">
                                {% set items_contenido_subtabs = item_contenido.entity.field_body_item_tab_multiple %}
                                  {% for item_contenido_subtabs in items_contenido_subtabs %}
                                   {% if item_contenido_subtabs.target_id %}
                                      {{ drupal_entity('paragraph', item_contenido_subtabs.target_id, check_access=false) }}
                                    {% endif %}
                                  {% endfor %}
                            </div>
                          {% endif %}
                        {% endfor %}
                      {% else %}
                       {% if item.entity.field_body_item_tab_multiple.target_id %}
                          {{ drupal_entity('paragraph', item.entity.field_body_item_tab_multiple.target_id, check_access=false) }}
                        {% endif %}
                      {% endif %}
                    </div>
                  </div>

                  {% if accordion_mobile_class %}
                    <div class="tab--accordion-mb">
                      <div class="cog--mq cog--flex">
                        {% for key_contenido ,item_contenido in items_contenido %}
                          {% if item_contenido.entity.bundle() == 'item_tab_multi' %}
                            <div class="js-accordion-row">
                              <div class="js-accordion">
                                <h3>{{ item_contenido.entity.field_name.value }}</h3>
                                <span class="icon-arrow"></span>
                              </div>
                              <div class="js-accordion-options sub-tab__container">
                                {% set items_contenido_subtabs = item_contenido.entity.field_body_item_tab_multiple %}
                                {% for item_contenido_subtabs in items_contenido_subtabs %}
                                  {% if item_contenido_subtabs.target_id%}
                                    {{ drupal_entity('paragraph', item_contenido_subtabs.target_id, check_access=false) }}
                                  {% endif %}
                                {% endfor %}
                              </div>
                            </div>
                          {% endif %}
                        {% endfor %}
                      </div>
                    </div>
                  {% endif %}
                </div>
              </div>
            {% endfor %}
          </div>
  </div>
</section>

