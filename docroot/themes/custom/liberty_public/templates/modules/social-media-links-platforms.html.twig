{#
/**
 * @ingroup themeable
 */
#}

{% set classes = [
  'social-media-links--platforms',
  'platforms',
  appearance.orientation == 'h' ? 'inline horizontal' : 'vertical'
] %}

<ul {{ attributes.addClass(classes) }}>
  {% for platform in platforms %}
    <li>
      <a href="{{ platform.url }}"
        class="{{ platform.name|lower }}"
        {{ platform.attributes }}>

      </a>
    </li>
  {% endfor %}
</ul>
