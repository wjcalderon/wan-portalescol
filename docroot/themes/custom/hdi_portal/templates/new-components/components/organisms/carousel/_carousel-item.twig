{% if item_display == 'card_basic' %}

  {% include "themes/custom/hdi_portal/templates/new-components/components/molecules/card/card-basic.twig" with {
    card__extra_classes: ['card-carousel__item'],
    card__heading: item.card__heading,
    card__body: item.card__body,
    card__heading: item.card__heading,
    card__button__content: item.card__button__content,
    card__button__url: item.card__button__url
  } %}

{% endif %}

{% if item_display == 'hero' %}

  {% include "themes/custom/hdi_portal/templates/new-components/components/molecules/hero/hero.twig" with {
    hero__extra_classes: ['hero__item'],
    hero__image: item.hero__image,
    hero__heading: item.hero__heading,
    hero__body: item.hero__body,
    hero__heading: item.hero__heading,
    hero__button__content: item.hero__button__content,
    hero__button__url: item.hero__button__url
  } %}

{% endif %}

{% if item_display == 'hero-breaker' %}

  {% include "themes/custom/hdi_portal/templates/new-components/components/molecules/hero/hero-breaker.twig" with {
    hero__extra_classes: ['hero__item'],
    hero__image: item.hero__image,
    hero__heading: item.hero__heading,
    hero__body: item.hero__body,
    hero__heading: item.hero__heading,
    hero__button__content: item.hero__button__content,
    hero__button__url: item.hero__button__url
  } %}

{% endif %}
